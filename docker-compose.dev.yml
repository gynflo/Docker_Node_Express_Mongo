version: "3.8"
services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - ./:/app
      - /app/node_modules
    env_file:
      - ./.env.dev
    command: npm run dev

  redis:
    env_file:
      - ./.env.dev
